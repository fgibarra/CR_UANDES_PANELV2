<?xml version="1.0" encoding="UTF-8"?>
<blueprint xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0"
    xmlns:camel="http://camel.apache.org/schema/blueprint"
    xmlns:cm="http://aries.apache.org/blueprint/xmlns/blueprint-cm/v1.1.0"
    xmlns:cxf="http://camel.apache.org/schema/blueprint/cxf"
    xmlns:jaxrs="http://cxf.apache.org/blueprint/jaxrs"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.osgi.org/xmlns/blueprint/v1.0.0 https://www.osgi.org/xmlns/blueprint/v1.0.0/blueprint.xsd        http://camel.apache.org/schema/blueprint https://camel.apache.org/schema/blueprint/camel-blueprint-2.20.2.xsd">
    <!-- xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx -->
    <!-- CXF servers -->
    <jaxrs:server address="/ESB/panelv2/servicio/sendmail/"
        id="rsApiSendMailServices" staticSubresourceResolution="true">
        <jaxrs:serviceBeans>
            <ref component-id="restService"/>
        </jaxrs:serviceBeans>
        <jaxrs:providers>
            <bean class="com.fasterxml.jackson.jaxrs.json.JacksonJsonProvider"/>
        </jaxrs:providers>
    </jaxrs:server>
    <!-- xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx -->
    <bean
        class="cl.uandes.panelv2.apiSendmail.api.resources.SendmailRestService" id="restService">
        <property name="smtp" value="{{serv.mailPanel.mail_smtp}}"/>
        <property name="ccDefault" value="{{serv.mailPanel.mail_cc}}"/>
        <property name="username" value="{{serv.mailPanel.mail_username}}"/>
        <property name="password" value="{{serv.mailPanel.mail_password}}"/>
        <property name="fromDefault" value="{{serv.mailPanel.mail_from}}"/>
    </bean>
    <bean class="cl.uandes.panelv2.apiSendmail.procesor.MailConAtachado" id="mailConAtachado">
        <argument value="false"/>
        <property name="smtp" value="{{serv.mailPanel.mail_smtp}}"/>
        <property name="username" value="{{serv.mailPanel.mail_username}}"/>
        <property name="password" value="{{serv.mailPanel.mail_password}}"/>
        <property name="port" value="{{serv.mailPanel.port}}"/>
        <property name="fromDefault" value="{{serv.mailPanel.mail_from}}"/>
    </bean>
    <!-- ==================================================================================================== -->
    <!-- ==================================================================================================== -->
    <camelContext id="sendmail-azure-v1" trace="false" xmlns="http://camel.apache.org/schema/blueprint">
        <propertyPlaceholder id="properties" location="file:${karaf.home}/etc/cl.uandes.panelv2.cfg"/>
        <route id="_readjson">
            <from id="_promService" uri="direct:start"/>
            <log id="_log1" message="queda en el body: ${body}"/>
            <choice id="_choice1">
                <when id="_when1">
                    <simple>${header.con-atachado} == 'SI'</simple>
                    <to id="_to1" uri="seda:con-atachado"/>
                </when>
            </choice>
            <to id="_final" uri="mock:end"/>
        </route>
        <route id="con-atachadoRoute">
            <from id="_from1" uri="seda:con-atachado"/>
            <process id="_process1" ref="mailConAtachado"/>
        </route>
    </camelContext>
</blueprint>
